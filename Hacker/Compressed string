function compressString(s) {
  let result = '';
  for (let i = 0; i < s.length; ) {
    let c = 1;
    while (
      i < s.length - 1 &&
      s[i] === s[i + 1]
    ) {
      c++;
      i++;
    }
    result +=
      s[i] + (c > 1 ? c : '');
    i++;
  }
  return result;
}
