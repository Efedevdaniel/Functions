function rot13(str) {
  return str.replace(
    /[a-zA-Z]/g,
    (char) => {
      const offset =
        char <= 'Z' ? 65 : 97;
      return String.fromCharCode(
        ((char.charCodeAt(0) -
          offset +
          13) %
          26) +
          offset,
      );
    },
  );
}
